The ``0-lookup`` module
================================

Using ``lookup``                                   
-----------------

Importing the function from the module:                            >>> lookup = __import__("0-lookup").lookup 
Checking for module docstring:
 	 >>> m = __import__("0-lookup").__doc__
         >>> len(m) > 1
         True

Checking for function docstring:
         >>> f = __import__("0-lookup").lookup.__doc__
	 >>> len(f) > 1
	 True
